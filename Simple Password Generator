/* La función de Google Apps Script, llamada pass_gen, está diseñada para generar una contraseña aleatoria de una longitud específica. Esta función acepta un parámetro:
len: Un entero que representa la longitud deseada de la contraseña generada. */

function pass_gen(len){
  const charset = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789!@#$%^&*()';
  let password = "";
  for(let i=0;i<len;i++){
    const randomIndex = Math.floor(Math.random()*charset.length);
    password += charset.charAt(randomIndex);
  }
  return password;
}
